language: python
python:
  - "3.6.2"
cache: pip

# install dependencies
install:
  - pip install -r requirements.txt
  - pip install coveralls

# run tests
script:
  - pytest tests/test_diary.py
  - coverage report

after_success:
  - coveralls